{
  "feature": "view-row-actions",
  "row_actions": {
    "type": "array",
    "items": {
      "type": "string",
      "pattern": "^[a-z0-9-]{1,48}$"
    },
    "maxItems": 64,
    "uniqueItems": true,
    "description": "Requires negotiated view-row-actions. Replaces model actions for this row; empty offers none, omission inherits."
  }
}
